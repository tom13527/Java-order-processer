/**
    This class will extent the ItemProcessing class. It will
    determine if the item is to be shipped to a consumer, then calculate the
    cost of shipping.

    @author Tom Good
*/
class ItemShipping extends CreateOrder {

    private String shipItem;
    private String yesShip = "y";

    // get method

    // set method

    /**
        This method will override the calculation method
        from the superclass and add shipping costs
    */
    public ..... {
        if (shipItem == yesShip) {

        } else {

        }
    }
}